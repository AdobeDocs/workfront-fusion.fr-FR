---
applyTo: "help/workfront-fusion/**"
source-git-commit: e3e28f86494207dd5e674a2882887d00da6a0c61
workflow-type: tm+mt
source-wordcount: '2352'
ht-degree: 1%
---

# Reconception de l’interface utilisateur de l’éditeur de scénario Fusion - Notes de projet

> Ce fichier est une copie synchronisée d’un fichier de mémoire du code Claude (`fusion-scenario-editor-redesign.md`) conservé afin que le copilote GitHub ait le même contexte. La copie de Claude est la source de vérité — chaque fois qu&#39;elle est mise à jour, ce fichier doit être mis à jour pour correspondre dans la même édition/validation. Si les deux ne sont jamais d&#39;accord, faites confiance au fichier de mémoire Claude et resynchronisez celui-ci.
>
> Il s’agit d’un fichier d’instructions Copilot étendu (`.github/instructions/*.instructions.md`), et non du `.github/copilot-instructions.md` à l’échelle du référentiel. Il n’est donc appliqué que lorsque Copilot travaille sur des fichiers sous `help/workfront-fusion/`, et non sur chaque requête du référentiel.

Becky documente la refonte de l’interface utilisateur de l’éditeur de scénarios de Fusion (nouvelle expérience vs. expérience classique) dans de nombreux articles. Il s’agit d’un **index dynamique** — mettez-le à jour (ne vous contentez pas de l’ajouter) chaque fois qu’une nouvelle capture d’écran est effectuée ou qu’un nouveau détail de l’interface utilisateur est confirmé, de sorte que toute personne (ou tout assistant) réalisant ce travail puisse répondre « avons-nous déjà une capture d’écran/un fait pour X ? » sans la dériver à nouveau.

Le travail se produit sur les `becky-updates-to-Fusion-scenario-editor` de branche. Feuille de calcul de suivi des Principal : `C:\Users\rebeccas\Desktop\Fusion scenario editor UI redesign - affected articles.xlsx` (feuille « Articles concernés » = statut/priorité par article ; feuille « Comparaison des fonctionnalités » = comparaison des fonctionnalités classiques par rapport aux nouvelles fonctionnalités).

Convention pour signaler tout ce qui n’est pas confirmé sur la ligne dans ces documents : `<!-- BECKY CHECK ME: ... -->` directement sur place, jamais comme une légende visible.

## Inventaire des captures d’écran (nouvelle expérience)

Réutilisation sur plusieurs articles via un chemin d’accès absolu plutôt que la duplication de fichiers, par exemple `/help/workfront-fusion/get-started-with-fusion/navigate-fusion/assets/run-once-new.png`.

**`help/workfront-fusion/get-started-with-fusion/navigate-fusion/assets/`** (à partir de la `scenario-editor.md` réécriture : icônes chromes de l’interface utilisateur partagée, réutilisées partout) :
`run-once-new.png` (Exécuter une fois, icône uniquement), `save-icon-new.png`, `notes-icon-new.png`, `scheduling-new.png` (bouton bascule de la barre inférieure de la planification + libellé de fréquence, par exemple « Immédiatement lorsque les données arrivent »), `auto-align-icon-new.png`, `scenario-editor-new.png` (zone de travail complète), `top-bar-new.png`, `controls-new.png`, `tools-new.png`, `favorites-new.png`, `ask-ai-new.png`, `canvas-navigation-new.png`, `search-modules-icon-new.png`, `find-and-replace-icon-new.png`, `snippets-icon-new.png`, `explain-flow-icon-new.png`, `export-blueprint-icon-new.png`, `export-as-headless-template-icon-new.png`, `import-blueprint-icon-new.png`, `previous-version-icon-new.png`, `devtool-icon-new.png`, `scenario-settings-icon-new.png`, `additional-controls-new.png`,,,,.

**`help/workfront-fusion/build-practice-scenarios/assets/`** (à partir des tutoriels sur les scénarios de base) :
`new-placeholder-module.png` (carte d’espace réservé de zone de travail vide), `new-connector-picker.png` (panneau de sélecteur d’application/de service - liste de catégories à gauche : Favoris, Toutes les applications, Adobe Workfront, Adobe Firefly Services, Adobe Creative et contenu, Adobe Cloud Services, IA générative, intégré ; recherche à droite), `new-renamed-module.png`, `new-map-toggle.png`, `new-map-id.png`, `new-execution-bubble.png`, `clock-icon-on-watch-record.png` (planning au niveau du module/badge d’horloge sur une carte), `new-map-in-filter.png` (configurer une fenêtre de filtre avec la condition d’ID de projet mappée), `new-mapped-update-record.png` (ID mappé dans le module « Mettre à jour un enregistrement »), `new-text-binary-function.png` (onglet du panneau de mappage « T »), `new-module-output-icon.png` (onglet du panneau de mappage en étoile), `new-mapped-name-block.png` `upper(...)` (bloc de nom mappé).

**`help/workfront-fusion/create-scenarios/add-modules/assets/`**:
`add-a-module-between-modules.png` (cliquez avec le bouton droit sur un chemin entre deux modules → menu : Configurer un filtre / Annuler le lien / Ajouter un routeur / Ajouter un module / Ajouter une note — capturée sur un chemin sans filtre / routeur pour le moment, il se peut donc que l&#39;option complète ne soit pas définie sur un chemin spécifique à un itinéraire ou un chemin porteur de filtre), `new-filter-setup-xml-example.png` (Configurer une fenêtre de filtre, correspond à l&#39;exemple File-Name-ends-with-.xml utilisé dans `add-a-filter-to-a-scenario.md`), `fallback-route-new.png` (Configurer une fenêtre de filtre, case à cocher d&#39;itinéraire de secours cochée et mise en surbrillance, utilisée dans router-module.md), `new-filter-specific-user.png` (Configurer une fenêtre de filtre sur un chemin, intitulé « Utilisateur spécifique », case à cocher de secours décochée condition, avec la case de surbrillance orange — utilisée dans le module de routeur.md&#39;s « Ajouter un filtre à une route dans la nouvelle expérience » pas à pas), `new-filter-specific-user-unmarked.png` (même filtre « Utilisateur spécifique », pas de case de surbrillance — utilisée comme la moitié `if` de l&#39;exemple if/else dans le module de routeur.md), `new-not-specific-user-unmarked.png` (Configurer une fenêtre de filtre, intitulée « Utilisateur non spécifique », case de secours cochée, pas de condition, pas de case de surbrillance — utilisée comme la moitié `else` de l&#39;exemple if/else dans le module de routeur.md).

**Pas encore obtenu** : aucun en attente pour router-module.md.

**`help/workfront-fusion/create-scenarios/config-error-handling/assets/`**:
`new-add-error-handler-in-menu.png` (cliquez avec le bouton droit sur un module → menu contextuel Paramètres : exécuter ce module uniquement / ajouter un gestionnaire d’erreurs / renommer / cloner / copier le module / ajouter une note / copier le mappage / copier le nom du module / métadonnées de l’application / supprimer le module — « ajouter un gestionnaire d’erreurs » en surbrillance — utilisé dans error-handler.md), `new-error-handling-directives.png` (le sélecteur d’application / de service s’est ouvert à partir d’un gestionnaire d’erreurs, avec une catégorie « gestion des erreurs » dédiée sélectionnée affichant les 5 directives — interrompre, valider, ignorer, reprendre, restaurer — à côté des catégories d’applications normales dans le panneau de gauche ; utilisé dans error-handler.md — réutilisé pour les flux de gestionnaire d — réutilisés pour les modules et les erreurs du routeur) `new-error-handler-examples-with-numbers.png` (exemple de scénario numéroté pour la présentation de la hiérarchie des gestionnaires d’erreurs, capturé dans **vue compacte** par le bouton (bascule) de la vue de la zone de travail du `scenario-editor.md` ; utilisé dans error-handle.md ; légende l’image en tant que vue compacte pour la lisibilité).

**`help/workfront-fusion/get-started-with-fusion/understand-fusion/assets/`**:
`new-scenario-example-unmarked.png` (capture d’écran source fournie par Becky, non marquée — scénario de synchronisation des utilisateurs Excel/Workfront à 8 modules : Recherchez l’utilisateur ajouté → Routeur → Recherchez l’utilisateur dans Workfront → Routeur → 3 branches [Définissez l’ID utilisateur existant / Créez un nouvel utilisateur dans Workfront → Définissez un nouvel ID utilisateur / Obtenez la variable d’ID utilisateur → Chargez l’ID utilisateur dans une feuille de calcul] — même scénario que l’entrée « Scénario » de `fusion-integration-example.png` ET `fusion-glossary.md` (`entire-scenario-blank.png`); conservez-la comme source pour toute autre mise en forme sur ce même exemple), `new-entire-scenario-scenario.png`, `new-scenario-trigger.png`, `new-scenario-module.png`, `new-scenario-route.png` (tous dérivés de la source non marquée ci-dessus via des zones de surbrillance rouges — consultez les notes par article pour l’emplacement exact de la case), (Fourni par Becky, cadre rouge autour de la liste d’applications dans le sélecteur du connecteur new-experience), `new-scenario-connectors.png` (Fourni par Becky, événements de surveillance Workfront à 2 modules → → Convertir le segment d’objet, encadré, suivi d’un module d’e-mail Microsoft 365 non encadré), `new-scenario-segment.png` (Fourni par Becky, non marqué, 4 modules Workfront uniquement — Obtention des informations sur le projet → Obtenir « affecté à » → Créer une mise à jour — même scénario que le `new-fusion-automation-example.png` de `fusion-template-example.png` ; également réutilisable dans la section « Exemple de Workfront Fusion pour l’automatisation du travail » de `license-automation-vs-integration.md` si Becky le souhaite également), `new-module.png` (recadrage de carte du module « Rechercher un utilisateur dans Workfront » de `new-scenario-example-unmarked.png`) via PowerShell/System.Drawing — utilisé de manière générique pour l&#39;entrée « Module » de `fusion-glossary.md`, pas de boîte rouge).

**`help/workfront-fusion/create-scenarios/config-scenarios-settings/assets/`**:
`new-scenario-settings-ex-1.png` (exemple de scénario à 2 modules : enregistrement de surveillance intitulé « Surveiller les demandes entrantes... ») → Action diverse intitulée « Convertir la demande en projet » — utilisée dans l’exemple configure-scenario-settings.md’s Nombre maximal de cycles), `new-max-number-cycles.png` (le panneau des paramètres du scénario s’ouvre sur la zone de travail, s’ouvre via l’icône d’engrenage dans la zone Contrôles, le champ « Nombre maximal de cycles » est mis en surbrillance avec la valeur `1` — utilisée dans configure-scenario-settings.md).

## Faits confirmés concernant la nouvelle expérience de l’interface utilisateur (aucune capture d’écran nécessaire pour les réutiliser)

- Pointez sur le bord droit d’un module → un bouton **Ajouter un autre module** apparaît → cliquez dessus → ouvre le sélecteur d’application ou de service (le même sélecteur que celui qui ajoute le premier module).
- Cliquez avec le bouton droit sur un module → menu contextuel Paramètres (ordre) : Exécuter ce module uniquement / **Ajouter un gestionnaire d’erreurs** / Renommer / Cloner / Copier le module / Ajouter une note / Copier le mappage / Copier le nom du module / Métadonnées de l’application / **Supprimer le module**. Les deux ont confirmé inchangé par rapport au classique.
- Cliquez avec le bouton gauche de la souris sur le chemin d’accès entre deux modules → la fenêtre **Configurer un filtre** s’ouvre directement. Cliquez avec le bouton droit sur le même chemin pour ouvrir un autre menu contextuel (plus complet), et non un menu avec l’option « Configurer un filtre ».
- Cliquez avec le bouton droit sur un chemin → **Ajouter un routeur** et **Ajouter un module** sont deux options (voir `add-a-module-between-modules.png`).
- **Copier le filtre** / **Coller le filtre** (en cliquant avec le bouton droit de la souris sur un chemin qui comporte déjà un filtre) existent toujours et fonctionnent de la même manière : avec un nouveau style visuel pour correspondre à la nouvelle zone de travail de la carte, mais avec les mêmes options/libellés.
- Cliquer sur le module Routeur lui-même (sans survoler son bord) ajoute toujours une nouvelle route — confirmée inchangée.
- **Ordonner les itinéraires** (cliquez avec le bouton droit sur le module Routeur → « Ordonner les itinéraires » → glisser-déposer) — confirmé toujours valide, inchangé par rapport à la version classique.
- **Désactiver l’itinéraire** (cliquez avec le bouton droit de la souris sur le chemin d’un itinéraire → « Désactiver l’itinéraire ») et le visuel désactivé-itinéraire (chemin gris + icône d’itinéraire désactivé sur le libellé) — confirmé inchangé par rapport à l’itinéraire classique.
- Le module Routeur (« Flow Control » > « Router ») est classé dans la catégorie **Intégré** du nouveau sélecteur (apparaît également sous « Toutes les applications », mais la catégorie Intégrée est celle à citer dans les instructions).
- Itinéraire de secours : contrairement à classic (qui le marque avec une flèche distincte sur le module Router lui-même), la nouvelle expérience marque l’itinéraire de secours en affichant **« Fallback »** en texte vert sur le libellé de l’itinéraire - aucune capture d’écran de marquage de flèche nécessaire pour ce fait.
- Itinéraire du gestionnaire d’erreurs : contrairement à l’itinéraire classique (cercles transparents ou pleins), la nouvelle expérience marque un itinéraire de gestionnaire d’erreurs par une ligne pointillée et un libellé **rouge « Gestionnaire d’erreurs »** aucune capture d’écran n’est nécessaire pour ce fait.
- Corrections de nom de module (noms de Workfront réels, et non fautes de frappe provenant d’anciens brouillons) : **« Enregistrement de contrôle »** (au singulier, et non « Enregistrements de contrôle ») **« Mettre à jour un enregistrement »** (et non « Mettre à jour un enregistrement »).
- Toujours non confirmé et bloquant `debug-a-scenario.md` + quelques autres articles : si le **DevTool** est supprimé de la nouvelle expérience (entendu comme une rumeur, aucun autre détail encore — voir la fiche de comparaison des fonctionnalités, ligne « DevTool »).

## Les conventions de la maison établies pour cette refonte (s&#39;appliquent à tous les autres articles)

- Duplication parallèle complète : une arborescence H2/H3 pour « ...dans la nouvelle expérience (recommandée) » et une arborescence distincte pour « ...dans l’expérience classique », avec des suffixes `(Classic)` partout où cela est nécessaire pour éviter les collisions d’ancres. Ajoutez une mini-table des matières sous n’importe quel en-tête qui comporte désormais plus de 2 enfants.
- Réduisez l’explication « nouveau ou classique » en une seule `>[!NOTE]` à la **fin de l’introduction de l’article** (et non de son propre H2), en réutilisant ce libellé exact, avec « dans l’éditeur de scénario d’article » ajouté au lien :
  > Workfront Fusion effectue la transition de l’éditeur de scénarios vers une nouvelle expérience . Au cours de cette transition, les expériences classique et nouvelle sont disponibles et vous pouvez basculer entre elles à tout moment. Nous vous recommandons d’utiliser la nouvelle expérience . Pour plus d’informations, voir [Expériences nouvelles et classiques](/help/workfront-fusion/get-started-with-fusion/navigate-fusion/scenario-editor.md#new-and-classic-experiences) dans l’éditeur de scénarios d’article.
- L’en-tête nouvelle expérience comporte des `(Recommended)` (par exemple, `## Add a filter in the new experience (Recommended)`).
- Le contenu véritablement indépendant de l’interface utilisateur (par exemple, la note de paramètre d’URL `?moduleId=` dans `add-a-module-basic.md`) n’a pas besoin d’être dupliqué. Faites preuve de jugement, mais vérifiez d’abord avec Becky si vous n’êtes pas sûr (elle a été repoussée auparavant lorsqu’une note a été déplacée sans demander).
- Si Becky confirme qu’une procédure entière est identique entre les expériences (et pas seulement une note indépendante de l’interface utilisateur), ne la divisez pas en H2/H3 nouveaux/classiques ; réduisez-la à une seule procédure en utilisant le libellé de la nouvelle expérience (par exemple, « Afficher le flux de données dans un scénario en cours d’exécution » de `view-scenario-data-flow.md`). L’hypothèse par défaut reste que les procédures diffèrent et doivent être fractionnées, et qu’elles ne s’effondrent qu’après confirmation explicite.
- Pour une seule capture d’écran illustrative (ce n’est pas une procédure complète détaillée) où les visuels nouveaux et classiques diffèrent véritablement, ne choisissez pas un ou patientez. Affichez d’abord les deux, en commençant par le nouveau : une ligne d’étiquette en texte brut (« Nouvelle expérience » / « Expérience classique ») juste au-dessus de chaque image, en commençant par la nouvelle expérience. Mentionnez d’abord le comportement de nouvelle expérience dans la prose environnante (par exemple, « ...marqué par X dans la nouvelle expérience, ou par Y dans l’expérience classique »), et pas seulement dans les libellés d’image. Voir les indicateurs d’exécution/de sortie de `view-scenario-data-flow.md`.
- Signalez tout ce qui n’est pas confirmé sur la ligne comme étant `<!-- BECKY CHECK ME: ... -->` à l’endroit ; jamais une légende visible.
- Une fois chaque indicateur d&#39;un article résolu, marquez cette ligne `Yes` dans la feuille de calcul de suivi « Articles affectés » (colonne G). Utilisez `In progress` si seuls certains indicateurs sont résolus.

## Statut par article (en date du 22 septembre 2026)

Terminé (0 indicateur, colonne G de la feuille de calcul marquée `Yes`) : `scenario-editor.md`, `create-basic-scenario.md`, `add-trigger-to-basic-scenario.md`, `add-filter-basic-scenario.md`, `add-a-webhook-to-basic-scenario.md`, `use-function-to-build-practice-scenario.md`, `add-a-module-basic.md`, `add-a-filter-to-a-scenario.md`, `router-module.md` (ligne 10), `error-handling.md` (ligne 15, restructurée en arborescence parallèle H3/H4 nouvelle/classique pour « Ajouter un gestionnaire d’erreur à un module » et « ...à un routeur »), `configure-scenario-settings.md` (ligne 20 — marque `Yes` dans la feuille de calcul de suivi, non encore mise à jour). « Ouvrez les paramètres du scénario » restructuré en une nouvelle division H2/H3/classique : la nouvelle expérience utilise la `scenario-settings-icon-new.png` existante (la zone Contrôles peut se trouver derrière l’icône des trois points) plutôt que l’icône d’engrenage classique. Le shadebox des exemples « Nombre maximal de cycles » utilise désormais des captures d’écran de nouvelle expérience `new-scenario-settings-ex-1.png` (exemple de scénario à 2 modules, objet Watch Record → Misc Action/Convert ) et `new-max-number-cycles.png` (panneau des paramètres du scénario ouvert via l’icône d’engrenage dans Contrôles, champ « Nombre maximal de cycles » mis en surbrillance). La troisième capture d’écran (`scenario-detail-350x207.png`) a été entièrement supprimée et remplacée par le texte suivant : « Vous pouvez voir les cycles déjà exécutés dans la zone Historique de la page Détails du scénario ».

`scenario-overview.md` (ligne 34 — marquer `Yes` dans la feuille de calcul de suivi, pas encore mis à jour ici) : terminé, 0 indicateur. Article purement conceptuel (pas d&#39;instructions étape par étape « cliquez sur X »), donc aucune nouvelle division H2/H3 classique n&#39;était nécessaire — juste ajouté la note de transition standard. Les 8 indicateurs d’origine ont tous été résolus :
- 4 (scénario complet, déclencheur, module, itinéraire) via des zones de surbrillance rouges dessinées avec PowerShell/System.Drawing sur le `new-scenario-example-unmarked.png` source non marqué de Becky (les coordonnées trouvées en recadrant et en zoomant la source plutôt qu&#39;en balayant les pixels — les approches flood-fill/pixel-loop dans PowerShell 5.1 ont continué à échouer sur les quirks tableau-contre-scalaire et étaient de toute façon trop lentes) : `new-entire-scenario-scenario.png`, `new-scenario-trigger.png`, `new-scenario-module.png`, `new-scenario-route.png`.
- 2 (connecteur, scénario-segment) via des captures d’écran Becky déjà marqué : `new-scenario-connectors.png`, `new-scenario-segment.png`.
- 1 (exemple d’intégration) résolu en confirmant `fusion-integration-example.png` (classique) correspond exactement au même scénario que `new-scenario-example-unmarked.png` : réutilisé directement, aucune nouvelle balise.
- 1 (exemple de modèle) résolu avec le `new-fusion-automation-example.png` non marqué de Becky, correspondant au `fusion-template-example.png` de Classic — également réutilisable dans la section « Exemple de Workfront Fusion for Work Automation » de `license-automation-vs-integration.md` si Becky le souhaite aussi.

`view-scenario-data-flow.md` (ligne 23 — marquez `Yes` dans la feuille de calcul de suivi, pas encore mis à jour ici) : terminé, 0 indicateur. Ajout de la note de transition standard. La confirmation « Afficher le flux de données dans un scénario en cours d’exécution » est identique dans les deux expériences (l’appel de Becky ; aucune division nouvelle/classique n’est nécessaire ici, contrairement à la plupart des autres procédures) ; il est réduit à une seule procédure à l’aide du libellé de nouvelle expérience (comprend l’étape « Cliquer n’importe où sur le scénario pour accéder à l’éditeur de scénario »), ce qui conserve la `assets/currently-running.png` de capture d’écran classique existante, puisque le panneau d’historique d’exécution est confirmé inchangé. Les deux indicateurs d’exécution/de sortie illustratifs DO diffèrent visuellement entre les expériences. Par conséquent, contrairement au modèle habituel de permutation de capture d’écran unique, les anciennes et les nouvelles captures d’écran sont affichées côte à côte, chacune avec un libellé en texte brut au-dessus (nouvelle expérience/expérience classique, nouvelle en premier) : l’indicateur d’exécution est `assets/new-spinning-icon.png` (tourne sur l’icône du module) par rapport à l’`assets/ring-around-module.png` classique (anneau croissant autour du module) ; l’indicateur de sortie est `assets/new-output-indicator.png` par rapport à l’`assets/data-flow-output.png` classique (même concept de bulle de comptage circulaire verte, restylisé). Ce motif « afficher les deux, étiqueté, nouveau d&#39;abord, étiqueter au-dessus de l&#39;image » est une convention de la maison — voir ci-dessus. Le texte du paragraphe descriptif lui-même mentionne également le comportement de nouvelle expérience en premier, puis classique (par exemple, « ...est marqué par un anneau qui tourne sur l’icône du module dans la nouvelle expérience, ou par un anneau qui grossit autour du module dans l’expérience classique. »). — le nouvel ordre s’applique en prose et pas seulement à l’image ou à l’étiquette.

`fusion-glossary.md` (ligne 43 — marquez `Yes` dans la feuille de calcul de suivi, pas encore mis à jour ici) : terminé, 0 indicateur. Tableau de glossaire pur, aucune procédure. Ajout de la note de transition standard avant le tableau. Les deux images incorporées (balises de `<img>` HTML brutes, et non markdown) ont été résolues sans avoir besoin de nouvelles captures d’écran fournies par l’utilisateur : le `entire-scenario-blank.png` de l’entrée « Scénario » était exactement le même scénario que `new-scenario-example-unmarked.png` (réutilisé directement) et le `module.png` de l’entrée « Module » a été remplacé par `new-module.png`, un recadrage de carte unique fabriqué à partir de la même source.

Bloqué (même traitement que `debug-a-scenario.md` — marqué, non modifié au-delà du drapeau, colonne G laissée vide) : `advanced-error-handling.md` (ligne 16). Ajout de la note de transition standard à l’introduction, mais ses deux exemples (« Exemple : gestion des erreurs avec des filtres » et l’exemple d’imbrication) sont un seul scénario Dropbox illustratif continu sans contenu « clic X » étape par étape à diviser en nouveau/classique ; les captures d’écran ont été délibérément laissées comme classiques, conformément à la consigne de Becky de ne pas les toucher au coup par coup. Indicateur inséré (juste avant « ### Exemple : gestion des erreurs avec des filtres » et juste avant le `>[!BEGINSHADEBOX]` d’imbrication) pour que l’article ait besoin d’un **tout nouveau scénario d’exemple créé dans la nouvelle expérience** (Becky a suggéré les modules Workfront au lieu de Dropbox), et pas seulement de remplacer des captures d’écran du scénario existant. Colonnes D et F de la feuille de calcul mises à jour avec la même note de portée ; la colonne G est laissée vide.

Non démarré : tout le reste de la feuille de calcul de tracking (`debug-a-scenario.md` bloqué lors de la confirmation de DevTool ; le reste est intact).
